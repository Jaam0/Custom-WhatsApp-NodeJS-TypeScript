import { message } from '../../services/whasApp.services';

describe('WhatSapp Service', () => {
  test('Message', () => {
    // const from = '18097020421@c.us';
    // const body = 'testing message';

    // const res = message(from,body);
    // expect(res).toBe(void 0);
  });
});
